# Because this next line has a wildcard value in it
# It should be the last Host value in this file, otherwise it 
# runs the risk of overriding other values, since SSH will just
# take the very first value that it finds. So more specific Host
# values should appear above this line.
Host *
    UseKeychain yes